on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write
  issues: read
  contents: read

jobs:
  check:
    uses: ./.github/workflows/check-description.yml
    with:
      mode: comment
    secrets: inherit
