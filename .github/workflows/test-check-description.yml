name: Check PR Description

on:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  pull-requests: write
  issues: write
  contents: write

jobs:
  check-description:
    uses: ./.github/workflows/check-description.yml
    with:
      mode: comment
    secrets: inherit
