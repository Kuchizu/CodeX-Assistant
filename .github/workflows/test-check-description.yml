name: Check Description

on:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  pull-requests: write
  issues: read
  contents: read

jobs:
  check-description:
    uses: ./.github/workflows/check-description.yml
    with:
      mode: draft
    secrets: inherit
